# echo "Starting to deploy docker image.."
# DOCKER_IMAGE=azeem0322/magnet_api
# docker pull $DOCKER_IMAGE
# docker ps -q --filter ancestor=$DOCKER_IMAGE | xargs -r docker stop
# docker run -d $DOCKER_IMAGE
